extends:
  - "../../../config/core"
  - "plugin:node/recommended-module"
  - "plugin:import/recommended"
  - "plugin:import/typescript"

settings:
  import/resolver:
    typescript:
      alwaysTryTypes: true

rules:
  unicorn/prefer-module: [off] # TypeScript to ESM Node.js is not working correctly
  node/no-missing-import: [off]
  import/extensions: [off]
